# Assets — replace with your personal files

## Memories (`/assets/memories/`)

Add photos named to match `src/data/memories.ts`:

- `memory-1.jpg` … `memory-5.jpg` — flash deck cards
- `carousel-1.jpg` … `carousel-6.jpg` — carousel strip

## Audio (`/assets/audio/`)

- `kun-faya-kun.mp3` — Kun Faya Kun (or your recording together)
- `whoosh.mp3` — optional blow sound
- `gift-open.mp3` — optional gift tap sound

## Video (`/assets/video/`)

- `birthday.mp4` — fullscreen birthday message video

Until you add files, images fall back to soft pink placeholders.
